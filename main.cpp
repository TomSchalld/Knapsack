#include <iostream>
#include <fstream>
#include <sstream>

#include "bruteforce.h"
int main() {

    /*ops *o = new ops();
    o->readInFile();
    std::cout << "Read in completed \n\n\n\n" << std::endl;
    o->print();
    delete(o);*/
    readIn();
    printOutInstance();
    return 0;
}